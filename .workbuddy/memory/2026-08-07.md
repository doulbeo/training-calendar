- 修复 iPhone 刘海/灵动岛安全区域避让：Index.tsx 和 Warmup.tsx 顶部按钮与状态栏重叠
  - 使用 `env(safe-area-inset-top)` + `max()` fallback 确保兼容所有机型
  - 构建通过
- 修复 Index.tsx sticky header 滚动穿透：z-10 → z-50，backdrop-blur-md 改为内联 backdropFilter + WebkitBackdropFilter（解决 iOS Safari 兼容）
- 推送到 GitHub origin/main：commit e77d118
